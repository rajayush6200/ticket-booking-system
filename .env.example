DATABASE_URL="file:../dev.db"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
PORT=4000
CLIENT_URL=http://localhost:5173

# Optional: configure SMTP for real email
# If not set, emails are simulated (logged to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
