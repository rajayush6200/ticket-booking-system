DATABASE_URL="file:../dev.db"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
PORT=4000
CLIENT_URL=http://localhost:5173

# EmailJS (server-side). If unset, emails are simulated (logged to console).
# Use one template for both booking confirmation and waitlist offers (free plan limit).
EMAILJS_PUBLIC_KEY=
EMAILJS_PRIVATE_KEY=
EMAILJS_SERVICE_ID=
EMAILJS_TEMPLATE_ID=
